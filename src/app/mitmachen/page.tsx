// Route intercepted by next-intl middleware at runtime.
// All requests to "/mitmachen" are served by src/app/[locale]/mitmachen/page.tsx.
// This file exists only to satisfy Next.js static analysis — it is never rendered.
// Do not add content here; it would drift out of sync with the localised page.
export default function MitmachenRoutePlaceholder() {
  return null
}
